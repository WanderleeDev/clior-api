export enum StockStatus {
  AVAILABLE = 'available',
  UNAVAILABLE = 'unavailable',
}

export enum Currency {
  USD = 'USD',
  PEN = 'PEN',
}

export enum Locale {
  USD = 'en-US',
  PEN = 'es-PE',
}
